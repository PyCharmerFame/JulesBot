name: Telegram to Jules

"on":
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: read

jobs:
  telegram-to-jules:
    runs-on: ubuntu-latest

    steps:
      - name: Read Telegram and trigger Jules
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
        run: |
          curl -s "https://api.telegram.org/bot${TG_BOT_TOKEN}/getUpdates" > updates.json

          TASK=$(jq -r --arg chat "$TG_CHAT_ID" '
            .result[]
            | select(.message.chat.id|tostring == $chat)
            | select(.message.text | startswith("/task "))
            | .message.text
          ' updates.json | tail -n 1 | sed 's#^/task ##')

          if [ -z "$TASK" ]; then
            echo "No /task message found"
            exit 0
          fi

          PROMPT="You are working in GitHub repository PyCharmerFame/JulesBot.

Task from Telegram:
${TASK}

Rules:
- Analyze the repository first.
- Make the smallest safe changes.
- Update README if needed.
- Create a Pull Request.
- Do not ask questions. Decide and proceed."

          curl -f 'https://jules.googleapis.com/v1alpha/sessions' \
            -X POST \
            -H "Content-Type: application/json" \
            -H "X-Goog-Api-Key: ${JULES_API_KEY}" \
            -d "$(jq -n \
              --arg prompt "$PROMPT" \
              --arg source "sources/github/${{ github.repository }}" \
              '{
                "prompt": $prompt,
                "sourceContext": {
                  "source": $source,
                  "githubRepoContext": {
                    "startingBranch": "main"
                  }
                },
                "automationMode": "AUTO_CREATE_PR",
                "title": "Telegram Task"
              }')"

          curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TG_CHAT_ID}" \
            -d text="✅ Задача отправлена в Jules. Жди Pull Request."
